Hello! It looks you accidentally submitted the test file (in the `tests` directory) instead of your source file (in the `src` directory). Can you submit the source file?

```bash
$ exercism submit src/lib.rs
```
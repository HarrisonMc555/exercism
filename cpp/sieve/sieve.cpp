#include <stdio.h>
#include <iostream>
#include "sieve.h"

using namespace std;

vector<int> primes(int max) {
    vector<int> primes = vector<int>();
    bool erat[max];
    erat[2] = true;
    return primes;
}

int main() {
    return 0;
}
